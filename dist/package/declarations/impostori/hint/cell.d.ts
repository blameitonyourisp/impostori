/**
 *
 * @param {Grid} grid
 * @param {number} index
 * @returns {GridCell}
 */
export function fillCellHints(grid: Grid, index: number): GridCell;
import { Grid } from "../../types/index.js";
import { GridCell } from "../../types/index.js";
