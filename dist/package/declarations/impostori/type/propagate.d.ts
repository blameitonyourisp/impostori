/**
 *
 * @param {GridCell} updatedCell
 * @param {Grid} grid
 * @returns {Grid}
 */
export function propagateCellType(updatedCell: GridCell, grid: Grid): Grid;
import { GridCell } from "../../types/index.js";
import { Grid } from "../../types/index.js";
