/**
 *
 * @param {Grid} grid
 * @returns {boolean}
 */
export function completedGridTypes(grid: Grid): boolean;
/**
 *
 * @param {Grid} grid
 * @returns {boolean}
 */
export function validateGridTypes(grid: Grid): boolean;
import { Grid } from "../../types/index.js";
